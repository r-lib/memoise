# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: required
r_binary_packages:
  - jsonlite
  - rex
  - devtools
  - httr
  - crayon
  - htmltools
r_packages:
  - covr

after_success:
  - Rscript -e 'covr::codecov()'
